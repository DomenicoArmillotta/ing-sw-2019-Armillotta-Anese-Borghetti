package it.polimi.ingsw.virtualview;

import it.polimi.ingsw.model.Worker;

public class Drawer {
    public void printWorker(Worker worker) {
        System.out.println("[VirtualView] Worker selezionato: " + worker);
    }
}
